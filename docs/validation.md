# Validation evidence

Executed by the assistant during the 19 September 2026 session, with JDK 17, Kotlin 2.1.21, and Gradle 8.11.1.

## Calculator milestone

- `test installDist`: BUILD SUCCESSFUL.
- EligibilityTest: 18 tests, zero failures, zero errors, zero skipped.
- FixtureTest: 5 tests, zero failures, zero errors, zero skipped.
- Total: 23 passing tests.
- One test evaluates 1,000 seeded input combinations, checking that less proceeds or more exposure cannot increase capacity, that the ceiling is respected, and that fee plus net cash equals principal. It also requires more than 100 cases to have positive capacity to avoid exercising only trivial zero-capacity cases.
- The packaged command-line app ran successfully. A separate Python JSON check compared its three result tuples with literal expected values: `(ELIGIBLE, 20000, 500, 19500, 0)`, `(HOLD, 0, 0, 0, 0)`, and `(NO_CAPACITY, 0, 0, 0, 4000)`. Amounts are cents, in principal/fee/net-cash/excess order.
- After the guided arithmetic review, a fourth fixture was added: $900 proceeds, $400 outstanding, $320 new principal, $8 fee, $312 net cash. The 23-test suite passed again, and the packaged demo's fourth output was checked against the literal `(32000, 800, 31200)` cent amounts. `build/decisions.json` now contains four records.

## Covered behavior

Hand-worked principal and fee examples; a refund reaching the exact limit; a refund creating excess exposure; fresh downloads with old coverage; exact report-date boundaries and optional delay allowance; holds with excess exposure; exposure ceiling; floor and half-up rounding; negative net adjustments; maximum Long values; invalid policy and temporal inputs; reporting time zone; deterministic replay; input completeness, fractional cents, unsupported currency, and decision JSON content.

## Deliberate fault check

Disabling the coverage hold caused the targeted test to fail on the expected business assertion. Correct code was restored before the final full run. Details: [mutation-check.md](mutation-check.md).

## Local artifacts

- `build/reports/tests/test/index.html`: generated test report.
- `build/test-results/test/`: JUnit XML results.
- `build/decisions.json`: actual packaged-demo output.
- `build/install/advance-eligibility-lab/`: packaged app.

Build artifacts are ignored by version control and can be regenerated. The calculator milestone alone did not validate persistence or concurrency. Fixtures and test expectations were written by the assistant; user code review is still outstanding.

## Transactional payment milestone

- `test integrationTest`: BUILD SUCCESSFUL. The original 23 tests and 21 PostgreSQL/HTTP integration tests passed: **44 total, zero failures, errors, or skipped tests**.
- Final `check installDist`: BUILD SUCCESSFUL; both test tasks were up to date from that passing run, and the distribution was packaged. Local Markdown links were checked with no missing targets.
- Real PostgreSQL transactions were exercised with concurrent requests, duplicate request keys, borrower limits across pools, and shared funding cash across developers.
- The HTTP simulator committed a payment and then truncated its response. Recovery retained reservations until verified evidence arrived, then recorded one funding journal without a second payment.
- During a risk hold, an inconclusive or not-found lookup retained reservations and caused no resubmission. A separate case introduced the hold during lookup and verified the second hold check.
- A child JVM halted after bank execution and before the local result commit. After lease expiry, recovery found the existing payment and posted it once.
- Additional tests covered stale worker fencing, definite rejection, destination changes, local transaction rollback after posting failure, immutable commands, and balanced append-only journals.
- A partial-repayment test uses a seeded snapshot to check that repayment does not recycle lifetime origination capacity. It does not implement or validate a reconciliation engine.

The first integration run exposed an actual response-body timeout defect. The agent inspected a thread dump, stopped the stalled test worker, and bounded the complete HTTP response future before rerunning successfully. See the [walkthrough](transactional-payments.md#defect-found-during-fault-testing).

`paymentDemo` also completed against the persistent Compose database. Its saved timeline (`build/payment-demo.json`, regenerated by `./gradlew paymentDemo`) records READY → UNKNOWN → SETTLED, one simulated bank transfer, one funding journal, and no additional effect on replay. The $200 principal reservation becomes a $195 bank debit plus $5 deferred fee. Recovery during the hold queries the existing transfer.

The demo PostgreSQL container was stopped afterward; its volume and schema data were preserved.

Additional artifacts:

- `build/reports/tests/integrationTest/index.html`: integration test report.
- `build/test-results/integrationTest/`: JUnit XML results.
- `build/payment-demo.json`: observed persistent demo timeline and schema names.

At this milestone, all money and source data were synthetic. No real bank or store integration, production underwriting performance, fraud-model accuracy, or end-to-end settlement reconciliation was validated. The app and simulator use separate transactions and schemas on one PostgreSQL server.

## Interactive dashboard milestone

`check` completed successfully with **72 tests, zero failures, errors, or skipped tests**:

| Suite                      | Tests |
| -------------------------- | ----: |
| EligibilityTest            |    18 |
| FixtureTest                |     5 |
| MonitoringTest             |     6 |
| ReservationIntegrationTest |    21 |
| DashboardIntegrationTest   |    22 |

The dashboard suite executes all 20 advertised scenarios through the real HTTP API against PostgreSQL and asserts resulting financial balances and states. It also checks malformed/oversized input, cross-origin and missing-header rejection, duplicate step requests, and concurrent allocation of the same collection receipt. The latter test verifies one posting, no duplicate repayment, unchanged lifetime funding, a closed pool that cannot originate again, and immutable collection history.

Monitoring tests exercise threshold boundaries and overflow, insufficient history, explicit FX rounding, prior-only baselines, source row counts and daily arithmetic, and reconciliation mismatches involving identity, currency, amount, duplicate evidence, missing bank debits, and missing ledger postings.

The UCI importer ran on the official archive: 541,909 source lines; 2,517 excluded; 305 derived days. Original file hashes and transformation rules are recorded in [data provenance](../data/README.md). The data are real retail observations, not app-store receivables or labeled fraud. Payment amounts remain simulated USD.

Browser review in Chrome confirmed scenario creation and execution, recovery to one settled payment, the balanced funding ledger, public-data chart rendering, date selection, a completed historical-day replay, and reopening that run through its local URL. Desktop layout was visually inspected. `node --check` passed for the browser JavaScript. This is not an automated browser regression suite or a claim of exhaustive mobile/accessibility testing.

The dataset importer was rerun from the saved official archive; its output was byte-for-byte identical. Final `processResources installDist` succeeded after the small UI fixes. Local Markdown links resolved. The local dashboard and Compose database were left running for review.

At the end of the dashboard milestone, the GitHub Actions workflow was prepared but had not run remotely. Current remote results are available in [GitHub Actions](https://github.com/aggarwalpulkit596/capital-lab/actions/workflows/check.yml). The dashboard is not deployed as a public web service.

## Engineering reference refactor

The local `check` passed with **84 backend tests, zero failures, errors, or skipped tests**: 18 eligibility, 5 fixture, 3 configuration/policy, 4 evidence/telemetry, 6 monitoring, 22 payment integration, and 26 dashboard integration tests.

Added coverage proves non-default financial terms remain consistent across quote, reservation, simulated bank, and ledger; a retry after policy replacement retains the frozen fee. Further checks cover invalid settings, configurable monitoring boundaries/explanations, out-of-order and concurrent evidence writes, read-only archives after restart, liveness/readiness, request identity, checksum drift, concurrent migration installation, and database lock timeouts.

The developer browser view was inspected locally and displayed $200 principal, a 2.5% / $5 fee, and $195 net payout. Finance layout was also visually inspected. Automated desktop/mobile browser regressions are defined in `tests/browser`; their authoritative results are the corresponding GitHub Actions run, not this local backend test count.

Dependency locks and SHA-256 verification metadata were generated. A normal verification run without write flags is required before merging. See the [engineering reference](engineering-reference.md) for implemented guarantees and remaining deployment work.
